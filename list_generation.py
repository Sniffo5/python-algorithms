import random
class Gen:
    def random(len, min = 1, max= 20000000):
        return(random.sample(range(min,max),len))
        
    def random_semi(len, randomness):
        pass
    def random_narrow(len):
        pass
    def sorted_reverse(len):
        
        pass